version: 2
jobs:
  build:
    docker:
      - image: python:3

    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install -r requirements.txt
      - run:
          name: Run tests
          command: python manage.py test stocks
      - run:
          name: MV cover
          command: mv cover $CIRCLE_ARTIFACTS